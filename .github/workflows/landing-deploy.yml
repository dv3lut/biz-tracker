name: Deploy biz-tracker Landing Page to VPS

on:
  push:
    tags:
      - 'v*'           # Release globale : déploie tous les services
      - 'landing/v*'   # Release ciblée landing uniquement
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./biz-tracker-landing-page
          file: ./biz-tracker-landing-page/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/biz-tracker-landing-page:${{ github.ref_name }}
            ${{ secrets.DOCKER_HUB_USERNAME }}/biz-tracker-landing-page:latest
          build-args: |
            VITE_APP_API_BASE_URL=${{ secrets.VITE_APP_API_BASE_URL }}

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          script: |
            if ! [ -x "$(command -v docker-compose)" ]; then
              sudo curl -L "https://github.com/docker/compose/releases/download/v2.29.6/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
              sudo chmod +x /usr/local/bin/docker-compose
            fi
            cd ~/biz-tracker-landing-page
            docker compose pull
            docker compose down
            docker compose up -d
