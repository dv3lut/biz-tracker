services:
  biz-tracker-db:
    image: postgres:16-alpine
    container_name: biz-tracker-db
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DATABASE__NAME}
      POSTGRES_USER: ${DATABASE__USER}
      POSTGRES_PASSWORD: ${DATABASE__PASSWORD}
    ports:
      - "${DATABASE__PORT:-15432}:5432"
    volumes:
      - biz_tracker_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${DATABASE__USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # biz-tracker-elasticsearch:
  #   image: docker.elastic.co/elasticsearch/elasticsearch:8.14.1
  #   container_name: biz-tracker-elasticsearch
  #   restart: unless-stopped
  #   environment:
  #     - discovery.type=single-node
  #     - xpack.security.enabled=false
  #     - bootstrap.memory_lock=true
  #     - ES_JAVA_OPTS=-Xms1g -Xmx1g
  #   ulimits:
  #     memlock:
  #       soft: -1
  #       hard: -1
  #   ports:
  #     - "9200:9200"
  #   volumes:
  #     - biz_tracker_elasticsearch_data:/usr/share/elasticsearch/data

  # biz-tracker-kibana:
  #   image: docker.elastic.co/kibana/kibana:8.14.1
  #   container_name: biz-tracker-kibana
  #   restart: unless-stopped
  #   depends_on:
  #     - biz-tracker-elasticsearch
  #   environment:
  #     - ELASTICSEARCH_HOSTS=http://biz-tracker-elasticsearch:9200
  #   ports:
  #     - "5601:5601"

  biz-tracker-mailhog:
    image: mailhog/mailhog:v1.0.1
    container_name: biz-tracker-mailhog
    restart: unless-stopped
    ports:
      - "1025:1025"
      - "8025:8025"


volumes:
  biz_tracker_postgres_data:
    driver: local
  biz_tracker_elasticsearch_data:
    driver: local
